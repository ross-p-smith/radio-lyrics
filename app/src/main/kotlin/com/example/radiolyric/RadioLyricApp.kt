package com.example.radiolyric

import android.app.Application
import dagger.hilt.android.HiltAndroidApp

/**
 * Hilt application root. Phase 1 stub — additional initialization (logging, Room
 * pre-warming, notification channels) lands in later phases.
 */
@HiltAndroidApp
class RadioLyricApp : Application()
